import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  textContainer: {
    paddingTop: 16,
    paddingBottom: 60,
  },
  text: {
    color: 'black',
    fontSize: 18,
    fontWeight: '800',
    textAlign: 'center',
  },
});
